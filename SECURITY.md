# 安全策略

## 支持范围

项目处于早期开发阶段，只维护默认分支的最新代码。

## 报告漏洞

请不要公开包含利用细节、密钥或个人数据的 Issue。优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能；若该功能尚未开启，请联系仓库维护者并仅提供最小复现信息。

报告中请包含受影响版本、复现步骤、影响范围和建议修复方式。维护者会在确认后协调披露时间。

## 部署边界

- Rust 网关当前没有实现身份认证，默认只监听 `127.0.0.1`。
- 不要将当前服务直接暴露到公网。
- AI Token 只应通过 App 设置写入 macOS Keychain。
- 日志、数据库和诊断信息可能包含股票代码、持仓笔记或使用记录，请按个人敏感数据处理。
